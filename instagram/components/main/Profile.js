import React from 'react';
import { Text, View } from 'react-native-web';

const Feed = () => {
    return (
        <View>
            <Text>Feed</Text>
        </View>
    );
};

export default Feed;
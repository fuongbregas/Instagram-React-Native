import React from 'react';
import { Text, View } from 'react-native-web';

const Profile = () => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    );
};

export default Profile;